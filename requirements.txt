# AFOS - fully pinned, exact-version requirements.
#
# Generated from `pip freeze` against the known-working environment in
# .venv/ (every Phase 0-5 smoke test and the full run_afos_demo.py
# integration run pass against exactly these versions). Pin, don't
# range-match: langgraph's checkpoint backends are split across several
# independently-versioned packages (langgraph-checkpoint is the core
# interface; langgraph-checkpoint-sqlite is the SQLite backend that
# core/memory_gateway/gateway.py's `from langgraph.checkpoint.sqlite import
# SqliteSaver` actually comes from) - an unpinned `pip install langgraph`
# pulls in langgraph + langgraph-checkpoint but NOT langgraph-checkpoint-sqlite,
# which would break that import with no code ever having changed. Pinning
# the full closure here is what prevents that class of drift.
#
# Install: pip install -r requirements.txt
# Verify:  python -c "from langgraph.checkpoint.sqlite import SqliteSaver; print('OK')"

aiosqlite==0.22.1
annotated-types==0.7.0
anyio==4.14.1
certifi==2026.6.17
charset-normalizer==3.4.9
colorama==0.4.6
distro==1.9.0
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.18
jiter==0.16.0
jsonpatch==1.33
jsonpointer==3.1.1
langchain==1.3.12
langchain-core==1.4.9
langchain-protocol==0.0.18
langgraph==1.2.8
langgraph-checkpoint==4.1.1
langgraph-checkpoint-sqlite==3.1.0
langgraph-prebuilt==1.1.0
langgraph-sdk==0.4.2
langsmith==0.10.1
openai==2.45.0
orjson==3.11.9
ormsgpack==1.12.2
packaging==26.2
pydantic==2.13.4
pydantic_core==2.46.4
python-dotenv==1.2.2
PyYAML==6.0.3
requests==2.34.2
requests-toolbelt==1.0.0
sniffio==1.3.1
sqlite-vec==0.1.9
tenacity==9.1.4
tqdm==4.68.4
typing-inspection==0.4.2
typing_extensions==4.16.0
urllib3==2.7.0
uuid_utils==0.17.0
websockets==15.0.1
xxhash==3.8.1
zstandard==0.25.0
